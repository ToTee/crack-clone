# AI 제공업체·모델과 충전액 수정

## 사용 방법

1. AI 모델 및 환경 설정에서 Claude, GPT, Gemini, DeepSeek 중 본문 AI를 선택합니다.
2. 해당 제공업체의 API 키를 입력하고 모델을 선택한 뒤 저장합니다. 빈 키 입력칸은 기존 키를 지우지 않습니다.
3. 기본 목록은 경량·일반·상위 텍스트 모델을 포함합니다. `API에서 모델 목록 새로고침`은 본인 계정에서 반환하는 추가 텍스트 모델과 버전을 가져옵니다. 목록 조회는 답변 생성 호출을 하지 않습니다.
4. `모델 ID 직접 입력`으로 새 모델을 지정할 수도 있습니다. 기본 목록과 계정 목록은 호출 성공을 보장하지 않습니다. 지원 종료·계정 권한·모델별 기능 제한에 따라 사용할 수 없는 모델도 있습니다.
5. 대화 기억 요약은 본문과 같은 AI·모델 또는 별도의 제공업체·모델을 선택할 수 있습니다. 본문 Claude + 요약 Gemini 같은 조합도 지원합니다. 기존 기억을 다시 요약하거나 기존 대화를 삭제하지 않습니다.

본문 프롬프트·분량·기억 보존 주기는 이 업데이트가 자동 변경하지 않습니다. Claude의 기존 캐시 구간 분리와 유지 시간 설정도 유지합니다. 다른 업체에는 Claude 캐시 설정을 보내지 않습니다.

## 충전액을 잘못 등록했을 때

`나의 별 → 해당 시작 잔액/충전 내역 → 상세 → 충전액 수정`에서 올바른 **USD 금액**을 입력하고 수정 저장합니다. 예를 들어 49,820별은 $49.82입니다.

기존 등록 금액을 수정하므로 새 충전으로 중복 반영하지 않습니다. 이후 잔액도 함께 다시 계산하고, 채팅 사용료 내역은 유지합니다. 0달러로 수정하면 해당 등록액이 잔액에서 빠집니다. 처음 등록한 금액과 수정 횟수를 표시하며 DB에 수정 이력을 남깁니다. 다른 화면에서 먼저 수정한 경우 덮어쓰지 않고 새로고침을 안내합니다.

## 별 계산

- 별은 실제 결제가 아닌 **앱의 통합 AI 사용 예산**입니다. 기존 별 잔액과 내역은 유지합니다. 이번 업데이트 이후 GPT·Gemini·DeepSeek 사용량도 기록합니다. 과거 GPT 사용분은 소급 차감하지 않습니다.
- $1 = 1,000별입니다. 제공업체가 반환한 사용 토큰을 표준 유료 단가로 계산합니다. 캐시 읽기·쓰기는 전체 입력 토큰에서 분리해 이중 계산을 막습니다. 추론 토큰은 제공업체의 출력 사용량에 포함된 값으로 처리합니다.
- 실제 제공업체 계정 잔액과 동기화하지 않습니다. 무료 등급·크레딧 할인·세금은 반영하지 않습니다. Gemini는 명시적 유료 캐시 저장 API를 별도로 만들지 않고 자동 캐시 사용량을 반영합니다.
- DeepSeek는 요청 시작 시각(UTC)을 기준으로 혼잡/비혼잡 요금을 추정합니다. 중국 공휴일 할인과 요청 중 요금 시간대 변경은 실제 청구와 차이가 날 수 있으며 해당 내역에 안내합니다.
- 단가가 없는 새 모델이나 사용량을 받지 못한 응답은 **계산 대기**로 남깁니다. 무료 사용으로 처리하거나 다른 모델 단가를 대입하지 않습니다. 계산 대기는 자동 정산을 뜻하지 않으며, 이후 단가·사용량 확인이 필요합니다.
- GPT 신형 모델의 272K 초과 입력과 Gemini Pro의 200K 초과 입력에 별도 단가를 적용합니다. 확인되지 않은 긴 입력 구간의 단가는 계산 대기로 남깁니다.

## 설치

기존 NAS 소스에 압축파일을 덮어쓰고 Container Manager에서 프로젝트를 다시 빌드·재생성한 뒤 화면을 새로고침합니다. **data 폴더와 기존 데이터 볼륨은 유지합니다.** 앱에 등록된 키·작품·대화·기억·별 기록은 해당 데이터 볼륨에 있습니다. 이 파일을 받는 것만으로 NAS에 적용되지는 않습니다.

## 확인한 공식 문서 (2026-09-21)

- Claude 모델: https://platform.claude.com/docs/en/about-claude/models/overview
- Claude 단가: https://platform.claude.com/docs/en/about-claude/pricing
- OpenAI 모델 및 개별 모델 단가: https://developers.openai.com/api/docs/models
- OpenAI 단가: https://developers.openai.com/api/docs/pricing
- OpenAI 캐시 토큰: https://developers.openai.com/api/docs/guides/prompt-caching
- Gemini 호환 API·모델 조회: https://ai.google.dev/gemini-api/docs/openai
- Gemini 단가: https://ai.google.dev/gemini-api/docs/pricing
- DeepSeek API: https://api-docs.deepseek.com/
- DeepSeek 단가: https://api-docs.deepseek.com/quick_start/pricing

## 검증 범위

TypeScript 검사, Next.js 빌드, 가짜 API 스트림과 별도 SQLite DB를 이용한 제공업체 연결·요약 분리·토큰 계산·충전액 수정 테스트, 실행 중인 Next.js의 설정/별 HTTP API 테스트를 수행했습니다. 실제 키로 유료 생성하거나 NAS에 설치하지 않았습니다. 브라우저 실행 파일 다운로드가 실패하여 실제 화면의 클릭/시각 검증은 완료하지 못했습니다.
