version: '3.8'

services:
  crack-clone:
    build: .
    container_name: crack-clone
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_DIR=/app/data
